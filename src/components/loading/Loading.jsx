import { ReactComponent as Spinner } from "../../assets/Spinner.svg"

const Loading = () => {
  return (
    <div>
        <h1>Loading</h1>
        <Spinner />
    </div>
  )
}

export default Loading